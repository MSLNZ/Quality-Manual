\DocumentMetadata{
    pdfversion=1.7,
    pdfstandard=A-3b
}

\documentclass{MSLQualityManual}
 
% ----- To allow cross-referencing between documents
%       a common root folder is identified.
%       Th following command encapsulates the folder path,
%       which is used to \input LaTeX source files.
\newcommand{\gptPath}{../guidelines_proficiency_testing}%

% ------ External references
\externaldocument[QM-]{../quality_manual_document/MSL_Quality_Manual}

\setTitle[Proficiency testing]{Guidelines on Proficiency Testing}
\setSubTitle

\begin{document}

% ----- These files contain the document content
\input{\gptPath/general}\clearpage

% ----- Creates the very last page, so no need for \clearpage
\input{references}	

\end{document}